# Single-process mode — no worker config files needed (example reference).
#
# This directory is the example counterpart of synapse/workers.singleprocess/.
# It is intentionally empty of YAML files because single-process mode requires
# no worker configurations.
#
# For workers mode templates, see synapse/workers.example/
